[pytest]
asyncio_mode = auto
testpaths = ../tests
python_files = test_*.py
python_functions = test_*
pythonpath = ../src
markers =
    real_cli: tests that shell out to a real binary (openspec, etc); skipped if missing
